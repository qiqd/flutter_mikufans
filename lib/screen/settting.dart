import 'package:flutter/material.dart';

class SetttingScreen extends StatelessWidget {
  const SetttingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.redAccent);
  }
}
